# version: '3.8'

# services:
#   elasticsearch:
#     image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
#     container_name: elasticsearch
#     environment:
#       - node.name=es-node
#       - cluster.name=es-docker-cluster
#       - discovery.type=single-node

#       # Enable security
#       - xpack.security.enabled=true
#       - xpack.security.enrollment.enabled=true

#       # Memory
#       - ES_JAVA_OPTS=-Xms1g -Xmx1g

#       # Set default password
#       - ELASTIC_PASSWORD=elastic123
#     ports:
#       - '9200:9200'
#     volumes:
#       - es_data:/usr/share/elasticsearch/data
#     networks:
#       - elastic

#   kibana:
#     image: docker.elastic.co/kibana/kibana:8.12.0
#     container_name: kibana
#     depends_on:
#       - elasticsearch
#     environment:
#       - ELASTICSEARCH_HOSTS=http://elasticsearch:9200

#       # Login with elastic user
#       - ELASTICSEARCH_SERVICEACCOUNTTOKEN=AAEAAWVsYXN0aWMva2liYW5hL2tpYmFuYS10b2tlbjpkbTFwbE80SFIzNnI4ODFCLVFtV0R3
#     ports:
#       - '5601:5601'
#     networks:
#       - elastic

# volumes:
#   es_data:

# networks:
#   elastic:

# docker exec -it elasticsearch bin/elasticsearch-service-tokens create elastic/kibana kibana-token

version: '3.8'

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
    container_name: elasticsearch
    environment:
      - node.name=es-node
      - cluster.name=es-docker-cluster
      - discovery.type=single-node

      # Enable security
      - xpack.security.enabled=true
      - xpack.security.enrollment.enabled=true

      # Memory
      - ES_JAVA_OPTS=-Xms1g -Xmx1g

      # Set default password
      - ELASTIC_PASSWORD=elastic123
    ports:
      - '9200:9200'
    volumes:
      - es_data:/usr/share/elasticsearch/data
    networks:
      - elastic

  kibana:
    image: docker.elastic.co/kibana/kibana:8.12.0
    container_name: kibana
    depends_on:
      - elasticsearch
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200

      # Login with elastic user
      - ELASTICSEARCH_USERNAME=elastic
      - ELASTICSEARCH_PASSWORD=elastic123
    ports:
      - '5601:5601'
    networks:
      - elastic

volumes:
  es_data:

networks:
  elastic:
